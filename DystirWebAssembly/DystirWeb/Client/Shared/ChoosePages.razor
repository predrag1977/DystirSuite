<div id="scroll_button_left_bottom" onclick="scrollOnMouseOverBottom('left')">
    &#10094;
</div>
<div id="horizontal_menu_bottom">
    <div id="horizontal_menu_wrapper_bottom">
        @foreach (var tab in TabList ?? new List<string>())
        {
            <div class="tab @(SelectedTab == tab ? "selected_tab" : "")" @onclick="(() => SetSelectedTab(tab))">
                <span>@(tab)</span>
            </div>
        }
    </div>
</div>
<div id="scroll_button_right_bottom" onclick="scrollOnMouseOverBottom('right')">
    &#10095;
</div>

@code {
    [Parameter]
    public EventCallback<string> TabOnClick { get; set; }

    [Parameter]
    public List<string> TabList { get; set; }

    [Parameter]
    public string SelectedTab{ get; set; }

    private void SetSelectedTab(string tab)
    {
        SelectedTab = tab;
        TabOnClick.InvokeAsync(tab);
    }
}
